# ================================
# LMEE - Paths Configuration
# ================================

BASE_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"

MODULES_DIR="$BASE_DIR/modules"
PRESET_DIR="$BASE_DIR/presets"
CONFIG_DIR="$BASE_DIR/config"
LOG_DIR="/var/log/lmee"

mkdir -p "$LOG_DIR"
